name: eGazette Monitor

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Test Run
        run: python -c "print('Hello from eGazette Monitor')"
